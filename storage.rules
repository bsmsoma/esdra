rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    function isAdmin() {
      return request.auth != null && request.auth.token.admin == true;
    }

    function isValidImage() {
      return request.resource != null &&
        request.resource.size < 8 * 1024 * 1024 &&
        request.resource.contentType.matches("image/(jpeg|png|webp)");
    }

    function isValidVideo() {
      return request.resource != null &&
        request.resource.size < 50 * 1024 * 1024 &&
        request.resource.contentType.matches("video/(mp4|webm|quicktime)");
    }

    match /assets/{fileName} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /produtos/{productId}/imagens/{fileName} {
      allow read: if false;
      allow create: if isAdmin() && resource == null && isValidImage();
      allow update: if false;
      allow delete: if isAdmin();
    }

    match /produtos/{productId}/videos/{fileName} {
      allow read: if false;
      allow create: if isAdmin() && resource == null && isValidVideo();
      allow update: if false;
      allow delete: if isAdmin();
    }

    match /{allPaths=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
